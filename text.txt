python -m django startproject myprojectname #(to start project)
python manage.py runserver #(to run server for newly created project , from manage.py file created in myprojectname)
python manage.py migrate (to enable django-admin)


creates views.py file in myprojectname
		from django.http import HttpResponse
		write def funname(request):
				RETURN HttpResponse(sssad)
Add paths to url.py file


write 'templates' in settings.py file.
from django.shortcuts import render


add {% csrf_token %} in post request after <form> tag.